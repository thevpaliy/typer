import React from "react";

const Metric = ({ count, title }) => (
  <div>
    <h3>{count}</h3>
    <h1>{title}</h1>
  </div>
);

const SessionScores = ({ words, chars, accuracy, seconds }) => null

export default SessionScores;
